一、选择题
1、下列程序中有几处错误？
#include<stdio.h>
int main(void)
{
  printf(Welcome to You！\n")
  return 0;
}
答：三处错误，修改后printf("Welcome to You!\n");

2、C语言中表达式（D）的值不等于1.
A.123/100 B.901%10 C.76%3 D.625%5

3、当i=2,j=3时，显示2*3=6
答：printf("%d * %d =%d\n",i,j,i*j);

4、x是double型变量，n是int型变量，若输入3 1.25后x=1.25，n=3怎样用scanf获取？
答：scanf("%d%lf",&n,&x);

5、下列运算符中，优先级最低的是谁？（B）
A.* B.= C.== D.%

6、观察代码并且补全。
if(x>=y){
  printf("max=%d\n",x);
}else{
  printf("max=%d\n",y);
}

7、程序总是从main函数开始执行的，说法正确。

二、填空题
1、k为整型变量，当计算1/k时结果的数据类型为（整型），计算1.0/k时的数据类型为（浮点型）
2、以下代码实现的功能是交换数据，输入3和2，输出结果为（a=2#b=3）
int a,b;
scanf("%d%d",&a,&b);
a=a+b;
b=a-b;
a=a-b;
printf("a=%d#b=%d\n",a,b);
3、完善代码实现使用temp交换a和b的值
int a,b,temp;
printf("Enter a,b:");
scanf("%d%d",&a,&b);
temp = a;
a = b;
b = temp;
printf("a=%d#b=%d\n",a,b);

4、假设n是整型变量，判断n是偶数的表达式是（n%2==0）
5、与数学表达式根号下s(s-a)(s-b)(s-c)对应的C语言表达式为（sqrt(s*(s-a)*(s-b)*(s-c))）
6、调用数学库函数时，编译预处理命名为（#include<math.h>），调用输入输出函数库时，编译预处理命令为（#include<stdio.h>）
7、算术、赋值、关系运算符的优先级为（关系＞算数＞赋值）
8、下列程序段输出的结果为（k=0#flag=0）
int k,flag;
if(k=0)
{
  flag=0;
}else{
  flag=1;
}
printf("k=%d#flag=%d\n",k,falg);

9、C语言三大基本的控制结构为（顺序结构，选择结构，循环结构）
10、下列程序段输出的结果是（）
